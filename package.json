{
  "name": "vitepress-project",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev interview",
    "build": "vitepress build interview",
    "serve": "vitepress serve interview",
    "deploy": "npm run build && gh-pages -d interview/.vitepress/dist",
    "dist": "vitepress build interview && npx rimraf docs && npx cpr interview/.vitepress/dist docs -o"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vitepress": "^1.6.4",
    "vue": "^3.5.26",
    "rimraf": "^6.0.1",
    "cpr": "^3.0.1"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "@algolia/client-search"
      ]
    }
  }
}
