# 说说JavaScript中的数据类型？
JavaScript是一种动态类型语言，这意味着变量可以随时赋予不同类型的值。JavaScript中的数据类型可以分为两大类：基本类型和引用类型。

基本类型

基本类型包括以下几种：

字符串（String）：用于表示文本数据。字符串可以用单引号或双引号括起来，例如"Hello"或'World'。
数字（Number）：表示整数和浮点数。JavaScript中只有一种数字类型，可以带小数点或不带小数点，例如42或3.14。
布尔（Boolean）：只有两个值：true和false，常用于条件判断。
空（Null）：表示一个空对象指针，只有一个值null。
未定义（Undefined）：表示变量未赋值，只有一个值undefined。
Symbol：ES6引入的一种新的原始数据类型，表示独一无二的值。
BigInt：用于表示任意精度的整数，可以安全地存储和操作大整数。

引用类型

引用类型包括以下几种：
对象（Object）：对象是键值对的集合，可以包含多个属性和方法。
数组（Array）：数组是对象的一种特殊形式，用于存储有序的元素集合。
函数（Function）：函数也是对象的一种，可以被调用执行特定的任务。
正则表达式（RegExp）：用于匹配字符串的模式。
日期（Date）：用于处理日期和时间。

动态类型
JavaScript具有动态类型，这意味着相同的变量可以用作不同的类型。例如：

```js
var x; // x 为 undefined
x = 5; // 现在 x 为数字
x = "John"; // 现在 x 为字符串
```

可以使用typeof操作符来查看变量的数据类型：
```js
typeof "John" // 返回 string
typeof 3.14 // 返回 number
typeof false // 返回 boolean
typeof [1,2,3,4] // 返回 object
typeof {name:'John', age:34} // 返回 object
```

# 如何判断JavaScript变量的类型？
在 JavaScript 中，判断变量类型是常见需求。不同方法适用于不同场景，下面给出一个综合示例：
```js
const dataList = [123, 'hello', true, null, undefined, [1, 2], {a: 1}, function(){}, new Date()];

// typeof
dataList.forEach(v => console.log(typeof v));
// instanceof
console.log([] instanceof Array); // true
// constructor
console.log((123).constructor === Number); // true
// Object.prototype.toString.call
console.log(Object.prototype.toString.call(null)); // [object Null]
```

1. typeof 运算符 用于检测原始类型（除 null 外）非常方便，返回字符串表示类型。
```js
typeof 123; // "number"
typeof null; // "object" (历史遗留问题)
typeof []; // "object"
typeof function(){}; // "function"
```
复制
优点：简单高效；缺点：无法区分对象与数组，null 判断错误。

2. instanceof 运算符 判断某对象是否为某构造函数的实例，通过原型链检测。
```js
[] instanceof Array; // true
new Date() instanceof Date; // true
123 instanceof Number; // false (原始类型不支持)
```
复制
适合引用类型判断，但跨 iframe 可能失效。

3. constructor 属性 每个对象的 constructor 指向其构造函数，可判断基本和引用类型。

```js
(123).constructor === Number; // true
({}).constructor === Object; // true
```
复制
注意：原型链被修改时可能不准确。

4. Object.prototype.toString.call() 最通用且精确的方法，返回 [object Type] 格式字符串。
```js
Object.prototype.toString.call([]); // "[object Array]"
Object.prototype.toString.call(null); // "[object Null]"
Object.prototype.toString.call(undefined); // "[object Undefined]"
Object.prototype.toString.call(new Date()); // "[object Date]"
```
复制
优点：可准确判断所有内置类型，不受原型修改影响；缺点：写法稍长。

💡 建议：

判断基本类型优先用 typeof。

判断数组用 Array.isArray() 或 toString.call()。

精确判断所有类型推荐 Object.prototype.toString.call()。